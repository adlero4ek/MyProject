datasets:

  GoalsAndStrategy.dataset.MainList:
    source: >
      (

        Goals.$spread().(
  
            $Goal_Id := $keys()[0];
  
            {
              "Goal_Id": $Goal_Id,
              "Goal": *.Goal,
              "Horizont": *.Horizont
            }
        )^(Horizont)
      
      )